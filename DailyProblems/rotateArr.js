// This problem was asked by Amazon.

// Given an array and a number k that's
// smaller than the length of the array, rotate the array to the right k elements in-place.

const arr = [1,2,3,4]; // [4,1,2,3];
const k = 3;

// arr[length - 1] = 3
// arr.length - k - 1 = 0

function rotateInPlace(arr, k) {
  
}

const result = rotateInPlace(arr, k);
console.log({ result });

