
// Node { next, data };

class Node {
  constructor(data, next=null) {
    this.data = data;
    this.next = next;
  }
}

// linked list methods - size, clear, getLast, getFirst
class LinkedList {
  constructor(head=null) {
    this.head = head;
  }

  size() {
    let count = 0;
  }
}