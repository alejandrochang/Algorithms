
// Problem
// Convert a string to an interger. You may assume that the string contains only digits and the minus character ('-').
// The second routine converts a singed integer stored as an int back to a string

function stringToInt(str) {
  
}


console.log(stringToInt('555')); // 555
console.log(stringToInt('-37')); // -37

function intToString(num) {

}

console.log(intToString(555)); // '555'
console.log(intToString(-37)); // '-37'