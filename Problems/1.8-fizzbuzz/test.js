const fizzbuzz = require('./fizzbuzz');

test("Function fizbuzz is declared", () => {
  expect(fizzbuzz).toBeDefined();
})

// test("To print Fizz for multiples of 3", () => {
//   expect(fizzbuzz).toBeDefined();
// })

// test("To print Buzz for multiples of 5", () => {
//   expect(fizzbuzz).toBeDefined();
// })

// test("To print FizzBuzz for multiples of both 3 and 5", () => {
//   expect(fizzbuzz).toBeDefined();
// })

// test("returns numbers when not divisble by 3 or 5", () => {
//   expect(fizzbuzz).toBeDefined();
// })

// come back to this test